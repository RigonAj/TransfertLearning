
dist = np.linalg.norm(eff - self.target)


reward = -dist


	# Mémorise la distance du step precedent
self.prev_dist = dist
	# Bonus quand l’agent se rapproche par rapport au step précédent
if hasattr(self, "prev_dist"):
    progress = self.prev_dist - dist
    reward += 3.0 * progress     # amplifie le signal de la bonne direction


if dist < 0.05:
    reward += 80.0



dist = np.linalg.norm(eff - self.target)

reward = -dist  

	# si trop proche de 0,0
dist_base = np.linalg.norm(eff - np.array([0.0, 0.0]))
if dist_base < 0.2:
    reward -= 2.0 * (0.2 - dist_base)   # punition progressive


if dist < 0.05:
    reward += 50.0


